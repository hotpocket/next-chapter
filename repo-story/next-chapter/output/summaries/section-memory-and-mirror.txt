The closing chapter covers the configuration and the memory the sessions ran under, both made readable to a reviewer who was never at the keyboard.

The centerpiece is the in-tree dot-claude mirror: checked-in copies of the machine-level config the agent actually ran, so paths cited in vault notes and prompt exports resolve to real files in the clone. The script scripts slash sync-claude-mirror generates it from the global config, the single source of truth: the orientation hook, the push-guard script, the global conduct file and settings, four skills in full, and the large gstack browser skill allowlisted to entry points only. Its roots are environment-derived, it fails loudly on a missing source, and a check mode gates every config commit against drift. The in-tree settings and skills are live, the push guard included; files suffixed dot-global are documentation only, unregistered so the global hooks do not double-fire. A tests-first suite exercises the script against a fake global directory in six phases.

For memory, a session-start hook prints three pointer lines, latest recap, open todos, open probes, and delegates reading to vault-digest, a headless grep-and-awk tool. The vault holds session recap-and-prompt pairs, todos, projects, a vet, and architecture decision records, superseded ones kept on purpose after a legibility audit; the pipeline's own model-split decision was superseded the day this chapter was recorded. One verbatim prompt from the July twentieth config-mirror session drives the whole mirror. With that, the build is auditable end to end.
